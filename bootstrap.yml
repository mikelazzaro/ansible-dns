---

- name: Finish configuration of bastion server
  hosts: localhost
  connection: local
  become: yes
  gather_facts: yes
  roles:
    - standard
    - user-friendly
    - bastion

- name: Configure central DNS server
  hosts: central-dns01
  become: yes
  gather_facts: yes
  roles:
    - standard
    - central-dns-server